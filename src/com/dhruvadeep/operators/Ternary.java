package com.dhruvadeep.operators;

public class Ternary {
    public static void main(String[] args) {
        // Variable = Expression ? true-part : false part
        String result;
        result = (10>=20) ? "TRUE" : "FALSE" ;
        System.out.println(result);
    }
}
